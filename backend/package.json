{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "npm install && node index.js",
    "dev": "nodemon index.js",
    "build": "npm install && echo 'No build step required' && npm run sentry:sourcemaps",
    "sentry:sourcemaps": "sentry-cli sourcemaps inject --org ishaan-ak --project node ./package.json && sentry-cli sourcemaps upload --org ishaan-ak --project node ./package.json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@sentry/cli": "^2.39.1",
    "@sentry/node": "^8.47.0",
    "@sentry/profiling-node": "^8.47.0",
    "bcrypt": "^5.1.1",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.5.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
